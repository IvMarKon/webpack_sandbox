//import fontawesome from '@fortawesome/fontawesome';
import * as fA from "./ico-pack";

fA.pushIcon('.container',fA.alignCenter);
fA.pushIcon('.container',fA.alignJustify);
fA.pushIcon('.container',fA.alignLeft);
fA.pushIcon('.container',fA.alignRight);

// fontawesome.dom.i2svg();
console.log("Webpack dev-server-watch test !");